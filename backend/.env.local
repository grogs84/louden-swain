# Local development environment with DuckDB
USE_DUCKDB=true
DUCKDB_PATH=wrestling.duckdb

# Dummy values for required settings (not used when USE_DUCKDB=true)
DATABASE_URL=postgresql://dummy
SECRET_KEY=your-secret-key-here-for-local-dev
SUPABASE_URL=https://dummy
SUPABASE_KEY=dummy
